1. 训练数据
  label index1:value1 index2:value2 ....
其中label表示该记录对应的类别(这里只有0和1两类)，后面的则是对应的特征下标以及相应的特征值

2. 测试数据
  id index1:value1 index2:value2 ....
其中id表示该条记录的id，后面的则与训练数据一样

3. 当value为0时，该对应项不会给出。如下的一条训练数据记录：
  0 1:0.4 5:1 ....
则这表示该记录属于0类，下标为1和5的特征对应的特征值分别为0.4和1，而下标为2~4的特征值则默认为0